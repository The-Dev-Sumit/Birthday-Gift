import React, { useRef, useEffect } from 'react';
import { gsap } from 'gsap';

const Balloon = React.memo(({ startX, startY, endX, endY }) => {
    return (
       <div>
             
       </div>
   
  );
})

export default Balloon;